# Domain Package QA Report

**Package:** `@app/domain`
**Date:** 2026-01-24
**Reviewer:** QA Agent
**Status:** Needs Attention

---

## Executive Summary

The domain package provides a solid foundation for the Body Recovery App with well-structured types, functional engines, and comprehensive constants. However, there are several issues that need to be addressed, particularly around type duplication, missing exports, and test coverage.

**Overall Assessment:** 7/10 - Good foundation with areas needing improvement

---

## 1. Types (`src/types/`)

### Files Reviewed
- `src/types/index.ts`
- `src/types/body.ts`
- `src/types/exercise.ts`
- `src/types/health.ts`
- `src/types/plan.ts`
- `src/types/safety.ts`
- `src/types.ts` (root-level duplicate)

### Findings

| Severity | File | Line | Issue | Suggested Fix |
|----------|------|------|-------|---------------|
| **CRITICAL** | `src/types.ts` | 1-330 | Duplicate type definitions at root level. This file duplicates types from `src/types/*.ts` with simplified versions that are inconsistent. | Remove `src/types.ts` entirely and use only `src/types/` directory. Update any imports. |
| **HIGH** | `src/types.ts` | 188-194 | `PlanType` in root types.ts has different values (`full`, `moderate`, `light`, `mobility_only`, `rest`) vs `src/types/plan.ts` (`full_workout`, `moderate_workout`, `light_movement`, etc.) | Standardize on the more descriptive values in `src/types/plan.ts` |
| **HIGH** | `src/types.ts` | 195-200 | `BlockType` simplified to only 4 values vs 13 values in `src/types/plan.ts` | Remove duplicate and use complete types |
| **MEDIUM** | `src/types.ts` | 142-162 | `Equipment` list is incomplete (10 items) vs `src/types/exercise.ts` (28 items) | Remove duplicate |
| **MEDIUM** | `src/types.ts` | 154-162 | `ExerciseCategory` missing `power` and `cardio` from complete types | Remove duplicate |
| **MEDIUM** | `src/types/exercise.ts` | 243-254 | `MuscleRecoveryEstimate` interface is exported but not used anywhere in the codebase except in constants | Consider if this should remain in types or move to constants |
| **LOW** | `src/types/body.ts` | 108-112 | `BodyRegionStatus.context` and `exerciseId` fields are optional but commonly used | Document expected usage patterns |
| **LOW** | `src/types/plan.ts` | 51 | `ExercisePrescription.reps` uses `number | string` union which may cause type checking issues | Consider using a discriminated union or branded type |

### Type Completeness Check

| Type Category | Status | Notes |
|--------------|--------|-------|
| Body Types | Complete | 34 regions, 12 sensation types, comprehensive injury tracking |
| Exercise Types | Complete | 15 patterns, 20 muscle groups, 28 equipment types |
| Plan Types | Complete | 7 plan types, 13 block types, comprehensive prescription |
| Health Types | Complete | Full daily signals, check-in types, readiness scoring |
| Safety Types | Complete | 7 action types, 4 severity levels, escalation UI |

---

## 2. Engines (`src/engines/`)

### Files Reviewed
- `src/engines/planning.ts`
- `src/engines/substitution.ts`
- `src/engines/safety.ts`
- `src/engines/index.ts`

### Planning Engine (`planning.ts`)

| Severity | Line | Issue | Suggested Fix |
|----------|------|-------|---------------|
| **MEDIUM** | 26-27 | Planning engine imports `MUSCLE_GROUPS` from constants but uses local casting that could fail at runtime | Add runtime validation |
| **MEDIUM** | 263-274 | Acute injury handling logic adds constraints to `regionsToAvoid` but doesn't check if region already exists in the array (potential duplicates) | Use Set or deduplicate explicitly |
| **MEDIUM** | 304 | `downgradeIfNeeded` has `rehab_focus` in order array but it's never generated by any rule | Either add rule to generate it or remove from order |
| **LOW** | 32-34 | `generateId()` uses `Date.now()` which could cause collisions in high-frequency scenarios | Consider using UUID or add more entropy |
| **LOW** | 526-534 | `rehab_focus` case in `buildPlanBlocks` is defined but never triggered | Add test coverage or remove dead code |
| **LOW** | 407-411 | `rehab_focus` case in `generateRationaleSummary` is defined but never triggered | Same as above |

**Logic Correctness:** The planning engine correctly implements:
- Readiness-based plan selection (5 tiers)
- Sleep modification rules (volume/intensity reduction)
- HRV modification rules (recovery-based adjustments)
- Pain/injury override rules (neurological symptoms detection)

### Substitution Engine (`substitution.ts`)

| Severity | Line | Issue | Suggested Fix |
|----------|------|-------|---------------|
| **MEDIUM** | 115-128 | `exerciseViolatesConstraint` function only checks 3 constraint types but there are 7 defined constraint types | Add handling for `limit_range`, `limit_volume`, `limit_frequency`, `modify_tempo`, `require_warmup` |
| **MEDIUM** | 293 | Empty function body in `calculatePrescriptionAdjustment` - always returns `undefined` | Implement prescription adjustment logic or document as intentional |
| **LOW** | 54-64 | Scoring weights sum to 130 with bonuses but documentation says "100 points base + bonuses" | Clarify documentation |
| **LOW** | 277 | Score capped at 115 but max theoretical with all bonuses is 125 | Adjust cap or weights |

**Logic Correctness:** The substitution engine correctly implements:
- Muscle overlap scoring (40 pts)
- Movement pattern similarity (25 pts)
- Equipment availability check (15 pts)
- Skill level matching (10 pts)
- Joint action overlap (10 pts)
- Bonus/penalty system for preferences

### Safety Engine (`safety.ts`)

| Severity | Line | Issue | Suggested Fix |
|----------|------|-------|---------------|
| **LOW** | 249 | Dead code path - first medium trigger handled but could have empty result | Add defensive check |
| **LOW** | 257 | Last else branch returns empty message for low triggers but they have messages | Verify intended behavior |

**Logic Correctness:** The safety engine correctly implements:
- 10 distinct red flag categories
- 4 severity levels (critical, high, medium, low)
- 7 action types with proper escalation
- Keyword-based detection AND body status detection
- Priority handling (critical > high > medium > low)

---

## 3. Constants (`src/constants/`)

### Files Reviewed
- `src/constants/index.ts`
- `src/constants/muscles.ts`
- `src/constants/patterns.ts`
- `src/constants/regions.ts`

### Findings

| Severity | File | Line | Issue | Suggested Fix |
|----------|------|------|-------|---------------|
| **LOW** | `muscles.ts` | 183-196 | `areMusclesSimilar` function has incomplete synergist expansion logic (only expands first set) | Consider bidirectional expansion |
| **LOW** | `patterns.ts` | 68 | `isometric` pattern has empty primary muscles array | Document that this is intentional for exercise-specific targeting |

### Completeness Check

| Constant Set | Status | Notes |
|-------------|--------|-------|
| BODY_REGIONS | Complete | 34 regions matching type definition |
| BODY_REGION_LABELS | Complete | All 34 labels present |
| BODY_REGION_GROUPS | Complete | 7 body groups |
| BODY_REGION_TO_MUSCLES | Complete | Mapping for all 34 regions |
| BILATERAL_PAIRS | Complete | All paired regions mapped |
| MUSCLE_GROUPS | Complete | 20 muscle groups |
| MUSCLE_GROUP_LABELS | Complete | All 20 labels |
| MUSCLE_RECOVERY_ESTIMATES | Complete | Light/moderate/heavy for all 20 groups |
| MUSCLE_SYNERGISTS | Complete | All 20 groups mapped |
| MUSCLE_ANTAGONISTS | Complete | All 20 groups mapped |
| MOVEMENT_PATTERNS | Complete | 15 patterns |
| MOVEMENT_PATTERN_LABELS | Complete | All 15 labels |
| PATTERN_PRIMARY_MUSCLES | Complete | All 15 patterns mapped |
| SIMILAR_PATTERNS | Complete | All 15 patterns have similar mappings |

---

## 4. Data (`src/data/`)

### Files Reviewed
- `src/data/index.ts`
- `src/data/exercises.ts`
- `src/data/profiles.ts`

### Findings

| Severity | File | Line | Issue | Suggested Fix |
|----------|------|-------|-------|---------------|
| **HIGH** | `exercises.ts` | 16-163 | Redeclares types already defined in `src/types/`. Creates maintenance burden and potential inconsistencies. | Import types from `../types` instead of redefining |
| **HIGH** | `profiles.ts` | 18-255 | Same issue - redeclares types already in `src/types/` | Import from `../types` |
| **HIGH** | `exercises.ts` | 118-120 | `Equipment` type includes `wall`, `chair`, `lacrosse_ball` not in main types | Sync with main types or extend properly |
| **MEDIUM** | `data/index.ts` | 78-85 | `EXERCISE_COUNTS` uses string filtering which won't work if category literal changes | Use type-safe approach |
| **MEDIUM** | `exercises.ts` | 141-163 | Local `Exercise` interface differs from `src/types/exercise.ts` (missing fields like `plane`, `loadingType`, `forceVector`, `complexity`) | Align interfaces or document differences |
| **LOW** | `exercises.ts` | - | Exercise IDs use inconsistent prefixes (`mob_`, `str_`, `stg_`, `act_`, `rec_`) | Document the naming convention |
| **LOW** | `profiles.ts` | 268-269 | Hard-coded dates will become stale | Consider relative dates or document as demo data |

### Data Quality

| Dataset | Count | Quality Notes |
|---------|-------|---------------|
| Mobility Exercises | 8 | Well-documented with cues and contraindications |
| Stretching Exercises | 8 | Complete with alternatives |
| Activation Exercises | 8 | Good muscle targeting |
| Strength Exercises | 10 | Progressive difficulty levels |
| Recovery Exercises | 8 | Includes breathing and foam rolling |
| **Total Exercises** | **42** | All have contraindications where appropriate |
| Demo Profiles | 3 | ACL Recovery, Low Back, Shoulder Rehab |

---

## 5. Tests (`__tests__/`)

### Files Reviewed
- `__tests__/planning.test.ts`
- `__tests__/safety.test.ts`
- `__tests__/substitution.test.ts`

### Test Results

```
Test Suites: 3 passed, 3 total
Tests:       70 passed, 70 total
```

### Coverage Report

| File | Statements | Branches | Functions | Lines |
|------|------------|----------|-----------|-------|
| **Overall** | **69.7%** | **66.19%** | **66.66%** | **71.48%** |
| Threshold | 80% | 80% | 80% | 80% |
| planning.ts | 85.35% | 78.57% | 88% | 85.71% |
| safety.ts | 95.4% | 86.48% | 100% | 94.8% |
| substitution.ts | 83.96% | 66.66% | 100% | 85.29% |
| muscles.ts | 58.62% | 50% | 33.33% | 68% |
| patterns.ts | 100% | 66.66% | 100% | 100% |
| regions.ts | 100% | 100% | 100% | 100% |
| exercises.ts | 0% | 0% | 0% | 0% |
| profiles.ts | 0% | 0% | 0% | 0% |
| types.ts | 0% | 100% | 100% | 0% |

### Coverage Issues

| Severity | Issue | Suggested Fix |
|----------|-------|---------------|
| **HIGH** | Overall coverage (69.7%) below threshold (80%) | Add tests for data modules |
| **HIGH** | `exercises.ts` has 0% coverage | Add unit tests for filter functions |
| **HIGH** | `profiles.ts` has 0% coverage | Add unit tests for profile helper functions |
| **MEDIUM** | `muscles.ts` only 58.62% statement coverage | Add tests for `areMusclesSimilar` |
| **MEDIUM** | `substitution.ts` only 66.66% branch coverage | Add tests for constraint type handling |
| **LOW** | `planning.ts` missing coverage for lines 263-274, 526-534 | Add tests for acute injury and rehab_focus paths |

### Missing Test Cases

1. **Planning Engine:**
   - Acute injury constraint handling
   - `rehab_focus` plan type generation
   - Multiple simultaneous pain regions

2. **Substitution Engine:**
   - All 7 constraint types
   - `calculatePrescriptionAdjustment` logic

3. **Data Modules:**
   - `getExerciseById`
   - `getExercisesByCategory`
   - `getExercisesByMuscleGroup`
   - `filterByEquipment`
   - `filterByContraindications`
   - `getAlternativesForContraindication`
   - `getDemoProfileById`
   - `getProfileContraindications`
   - `getRecommendedCategories`

---

## 6. Exports (`src/index.ts`)

### Findings

| Severity | Issue | Suggested Fix |
|----------|-------|---------------|
| **HIGH** | Data exports (`EXERCISES`, `DEMO_PROFILES`, utility functions) are NOT exported from main index | Add data exports or create separate data entry point |
| **MEDIUM** | No re-export of data module utilities like `getExerciseById`, `filterByEquipment` | Add to public API if needed |
| **LOW** | Comment block could document package better | Add JSDoc with examples |

### Current Public API

**Types Exported:** 55+ types across body, exercise, plan, health, safety categories

**Engines Exported:**
- `PlanningEngine`, `planningEngine`, `IPlanningEngine`
- `SubstitutionEngine`, `substitutionEngine`, `ISubstitutionEngine`, context types
- `SafetyEngine`, `safetyEngine`, `ISafetyEngine`, `redFlags`, `safetyKeywords`

**Constants Exported:**
- All body region constants (5 exports)
- All movement pattern constants (8 exports)
- All muscle group constants (8 exports)

**Missing Exports:**
- `EXERCISES`, `EXERCISES_BY_ID`
- `DEMO_PROFILES`, `DEMO_PROFILES_BY_ID`
- Data utility functions (10+ functions)

---

## 7. Recommendations Summary

### Critical Priority (Fix Immediately)
1. Remove `src/types.ts` - duplicate definitions causing confusion
2. Align data module types with main types
3. Increase test coverage to meet 80% threshold

### High Priority (Fix Soon)
1. Add data exports to main index.ts
2. Add tests for data utility functions
3. Sync `Equipment` type between files
4. Implement `exerciseViolatesConstraint` for all constraint types

### Medium Priority (Plan for Next Sprint)
1. Implement `calculatePrescriptionAdjustment` in substitution engine
2. Add tests for acute injury handling paths
3. Add validation for exercise ID generation
4. Document `rehab_focus` plan type usage or remove

### Low Priority (Tech Debt)
1. Update documentation strings
2. Add JSDoc examples for public API
3. Consider relative dates in demo profiles
4. Standardize exercise ID prefixes

---

## Appendix: File Structure

```
packages/domain/
├── __tests__/
│   ├── planning.test.ts      (70 tests passing)
│   ├── safety.test.ts
│   └── substitution.test.ts
├── src/
│   ├── constants/
│   │   ├── index.ts
│   │   ├── muscles.ts
│   │   ├── patterns.ts
│   │   └── regions.ts
│   ├── data/
│   │   ├── index.ts
│   │   ├── exercises.ts      (42 exercises)
│   │   └── profiles.ts       (3 demo profiles)
│   ├── engines/
│   │   ├── index.ts
│   │   ├── planning.ts
│   │   ├── safety.ts
│   │   └── substitution.ts
│   ├── types/
│   │   ├── index.ts
│   │   ├── body.ts
│   │   ├── exercise.ts
│   │   ├── health.ts
│   │   ├── plan.ts
│   │   └── safety.ts
│   ├── index.ts              (main entry)
│   └── types.ts              (DUPLICATE - REMOVE)
├── jest.config.js
├── package.json
└── tsconfig.json
```

---

**Report Generated:** 2026-01-24
**Next Review Recommended:** After addressing Critical and High priority items
